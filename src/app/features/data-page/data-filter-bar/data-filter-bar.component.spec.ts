import { ComponentFixture, TestBed } from '@angular/core/testing';

import { DataFilterBarComponent } from './data-filter-bar.component';

describe('DataFilterBarComponent', () => {
  it('placeholder', () => {
    expect(true).toBeTrue();
  });
});
